parameters:
    # app.param: 'valeur'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Controller\FrontController:
        bind:
            string $projectDir: '%kernel.project_dir%'
